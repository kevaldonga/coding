#include<iostream>
using namespace std;
int main(void){
    bool op=false;
    op==true ? cout<<"its right " : cout<<"its wrong ";
    // true ? cout<<"its right " : cout<<"its wrong ";  it means that if conditon is true it will print first result
return 0;
}